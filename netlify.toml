[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

# Next.js plugin for Netlify (automatically handles routing and serverless functions)
[[plugins]]
  package = "@netlify/plugin-nextjs"

